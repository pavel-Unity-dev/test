public class InteractionPromptData
{
    public bool IsVisible;
    public string Text;

    public InteractionPromptData(bool isVisible, string text)
    {
        IsVisible = isVisible;
        Text = text;
    }
}